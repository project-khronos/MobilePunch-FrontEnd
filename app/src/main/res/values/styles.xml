<resources>

  <!-- Base application theme. -->
  <style name="AppTheme" parent="Base.Theme.AppCompat.Light.DarkActionBar">
    <!-- Customize your theme here. -->
    <item name="colorPrimary">@color/rallyDarkGreen</item>
    <item name="colorPrimaryDark">@color/rallyGreen</item>
    <item name="colorAccent">@color/rallyBlue</item>

    <item name="android:textColor">@color/rallyDarkGreen</item>


  </style>

  <style name="AppTheme.NoActionBar">
    <item name="windowActionBar">false</item>
    <item name="windowNoTitle">true</item>
  </style>

  <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar"/>

  <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light"/>
  <style name="spinnerItemStyle" parent="@android:style/Widget.Holo.DropDownItem.Spinner">
    <item name="android:textColor">@color/rallyBlue</item>
    <item name="android:background">@color/rallyGreen</item>
  </style>

</resources>
